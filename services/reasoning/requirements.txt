# akshu-ai/services/reasoning/requirements.txt

fastapi==0.111.0
uvicorn==0.30.1
spacy==2.3.7 # Or a more recent compatible version if needed
en_core_web_sm==2.3.1 # Or a more recent compatible version
# Future dependencies will include libraries/logic for reasoning and planning (e.g., specialized planning algorithms, potentially interaction with LLMs via API calls)